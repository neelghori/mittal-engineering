# Only exclude files that should never be deployed
# Note: uploads/ is excluded from function bundle via vercel.json excludeFiles,
# but should still be deployed as static assets
.git/
node_modules/
*.sql
database/

